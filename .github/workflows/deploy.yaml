name: "Deploy Subgraphs"

on:
  workflow_dispatch:
    inputs:
      network:
        description: "Network name in lowercase"
        required: true

jobs:
  deploy:
    runs-on: "ubuntu-latest"
    steps:
      - name: "Check out the repo"
        uses: "actions/checkout@v2"

      - name: "Install Node.js"
        uses: "actions/setup-node@v2"
        with:
          cache: "yarn"
          node-version: "16"

      - name: "Install dependencies"
        run: "yarn install --immutable"

      - name: "Lint the code"
        run: "yarn lint"

      - name: "Generate the subgraph manifest"
        run: "yarn prepare:mainnet"

      - name: "Generate the AssemblyScript types"
        run: "yarn codegen"

      - name: "Deploy to ${{ github.event.inputs.network }}"
        run: yarn deploy:${{ github.event.inputs.network }} --access-token ${{ secrets.THE_GRAPH_TOKEN }}
